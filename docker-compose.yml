version: '3'
services:
  messagelogclient:
    image: "sebug/messagelog-client:324"
    ports:
     - "9090:80"
    env_file:
     - ./messagelogclient.env
  logdb:
    image: "sebug/postgresql-messagelog:324"
    env_file:
     - ./database.env
    volumes:
     - db-data:/var/lib/postgresql/data

volumes:
  db-data:
    driver: local